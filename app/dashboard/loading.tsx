import GigLoader from "@/components/(main)/GigLoader";
import React from "react";

const LoadingSpinner = () => {
  return <GigLoader color="border-yellow-400" size={"lg"} />;
};

export default LoadingSpinner;
